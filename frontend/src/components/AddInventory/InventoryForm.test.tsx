import { describe, expect, it } from "vitest"
import { render, screen, fireEvent } from "@testing-library/react"
import InventoryForm from "./InventoryForm";

describe("fill inventory form", () => {
  it("does nothing", () => {
    expect(0).toBe(0)
  })
})